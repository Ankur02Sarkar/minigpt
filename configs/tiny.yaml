# ~8M param sanity model (Phase 4.2 / local overfit helper)
model:
  vocab_size: 32000
  num_layers: 6
  hidden_size: 256
  num_heads: 4
  max_position_embeddings: 512
  dropout: 0.1
  rope_theta: 10000.0
  tie_weights: true
